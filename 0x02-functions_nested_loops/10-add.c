#include "main.h"
/**
 * add - sums two digits and returns the value
 * @num1: first argument
 * @num2: second argument
 * Return: returns sum of two numbers
 */
int add(int num1, int num2)
{
	return (num1 + num2);
}
