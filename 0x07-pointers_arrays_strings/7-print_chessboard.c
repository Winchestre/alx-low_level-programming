#include "main.h"

/**
 * print_chessboard - Entry point
 * @a: Array arg
 * Return: Always 0
 */
void print_chessboard(char (*a)[8])
{
	int i, j;

	for (i = 0; i < 8; i++)
	{
		for (n = 0; n < 8; j++)
			_putchar(a[i][j];
		_putchar('\n');
	}
}
